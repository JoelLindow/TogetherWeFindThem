<h3> Mailbox </h3>

<small><%= link_to 'Send Message', new_message_path, class: 'btn btn-lg btn-primary' %></small>

<ul class="list-group">
  <%= render partial: 'conversations/conversation', collection: @conversations %>
</ul>

<%= will_paginate %>